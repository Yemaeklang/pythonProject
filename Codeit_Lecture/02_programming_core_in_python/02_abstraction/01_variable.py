# 변수 제대로 이해하기

name = "이재필"  # 변수 name에 "이재필"을 지정
x = 7  # 변수 x에 7을 지정

print(f"name = {name}, x = {x}")

x = x + 1  # =은 지정연상자
# 변수 x의 값이 기존 변수 x의 값 + 1 이 된다.

print(f"name = {name}, x = x + 1 일 때, x = {x}")
